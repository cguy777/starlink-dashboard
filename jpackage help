This is how I build after exporting from eclipse using the StarlinkDashboard class as the run configuration.
This might be different if you are directly building using maven.

jpackage --input .\folder-containing-jarfile --name StarlinkDashboard --main-jar StarlinkDashboard.jar --main-class org.eclipse.jdt.internal.jarinjarloader.JarRsrcLoader --type app-image